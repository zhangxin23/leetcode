#include <iostream>
#include <stack>
#include <ctype.h>

using namespace std;

char* readNext(const char *ptr)
{
	return null;
}

char* mid2post(const char* ptr)
{
	char* rtnBuf = (char*)malloc(strlen(str) * sizeof(char));
	stack<char> op_statck;
	char* entry = readNext(ptr);
	if(!isdigit(entry[0])) {

	} else {
		
	}

}

int main()
{

}